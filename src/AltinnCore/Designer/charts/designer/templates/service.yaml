apiVersion: v1
kind: Service
metadata:
  name: {{ template "designer.fullname" . }}
  labels:
    app: {{ template "designer.name" . }}
    chart: {{ template "designer.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "designer.name" . }}
    release: {{ .Release.Name }}
